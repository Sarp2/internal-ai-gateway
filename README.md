# internal-ai-gateway

## Development

Install dependencies:

```bash
pnpm install
```

Run lint and format checks:

```bash
pnpm lint
```

Run TypeScript checks:

```bash
pnpm check
```

Run tests:

```bash
pnpm test
```

Synthesize the CDK app:

```bash
pnpm synth
```
